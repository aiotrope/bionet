<div class="hero" role="img" aria-label="Image Description">
  <h1 class="author">Arnel Imperial</h1>
  <p class="hero-text">Frontend & Backend Web Developer</p>
</div>

<style>
  .hero {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    text-align: center;
    width: 100%;
    height: 75vh; /* if you don't want it to take up the full screen, reduce this number */
    overflow: hidden;
    background-size: cover !important;
    margin-bottom: 0;
    background: radial-gradient(
        ellipse at center,
        rgba(0, 0, 0, 0) 0%,
        rgba(0, 0, 0, 0) 37%,
        rgba(0, 0, 0, 0.65) 100%
      ),
      url(../../hero.svg) no-repeat center center scroll;
  }
  .author {
    font-style: normal;
    font-weight: bold;
    color: var(--color-offwhite);
    font-size: 7vmin;
    letter-spacing: 0.03em;
    line-height: 1;
    text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.8);
    margin-bottom: 10px;
  }

  .hero-text {
    color: var(--color-offwhite);
    font-size: 3vmin;
  }
  @media (max-width: 480px) {
    .author {
      font-size: 6vh;
    }
    .hero-text {
      font-size: 3vh;
    }
  }
</style>
